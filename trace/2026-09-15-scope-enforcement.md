# 2026-09-15 — SCOPE ENFORCEMENT

bound PATH steps to file scopes and made Gate 2 verify them against the real git diff; added `allowed_paths`/`forbidden_paths` step fields (`PATH.md`), scope matcher and rule parser (`scripts/path_scope.sh`), Gate 2 wiring (`scripts/gate_enforce.sh`), mirrored enforcement in the extension (`vscode-extension/src/parsers.ts`, `vscode-extension/src/gates.ts`), updated emitted templates (`vscode-extension/src/templates.ts`), added `governance.diffBase` setting (`vscode-extension/package.json`), documented the criterion (`GATE.md`, `README.md`), added tests and `make test` (`scripts/test_path_scope.sh`, `scripts/test_extension_parity.mjs`, `Makefile`), refreshed state (`REALITY.md`); deviations: PATH scope widened twice during execution (P4 to add `vscode-extension/package.json`, P3 to add `Makefile`), both recorded in `PATH.md` as notes rather than taken silently; evidence: 27 shell assertions and 17 parity assertions PASS, `tsc --noEmit` clean, extension build clean; gate_1=PASS (route declared and bounded before work), gate_2=PASS (all changed files inside declared allowed_paths).
