# 2026-09-15 — CI GATE

put the gates in CI so admissibility is checked on every pull request rather than only when someone remembers to run `make gate`; added `.github/workflows/governance-gate.yml`, which runs both gates and the test suite and posts the verdict as a pull request comment updated in place via a hidden marker; added `scripts/gate_report.sh` to render the Markdown so the rendering is testable rather than buried in workflow YAML, plus `scripts/test_gate_report.sh` and a `make report` target; removed the undeclared ripgrep dependency from `scripts/gate_enforce.sh` in favour of `grep`, since under `set -e` a missing `rg` aborted the gate instead of reporting a clean FAIL and in CI would have read as a broken job; files: `.github/workflows/governance-gate.yml` (added), `scripts/gate_report.sh` (added), `scripts/test_gate_report.sh` (added), `scripts/gate_enforce.sh`, `Makefile`, `README.md`, `PATH.md` steps P19-P24, `REALITY.md`; deviations: none, all changes fell inside scopes declared before work; evidence: 19 report assertions, 13 decision-log assertions, 14 append-only assertions, 27 scope assertions and 26 parity assertions PASS, the grep rewrite re-verified against placeholder, blocking-question and active_step violations to confirm the conditions still fire, and the full CI path simulated locally against the real merge-base with rc=0; two limits recorded in `REALITY.md`: the comment cannot post from a fork where the token is read-only, and the `push` run on `main` assumes the merged PATH still describes that commit; gate_1=PASS (scope declared before work in steps P19-P24), gate_2=PASS.
